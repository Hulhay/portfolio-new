const HeadingComponent = ({ title }) => {
  return (
    <h2 className="text-[30px] text-orange-web py-[10px] px-11 self-center lg:text-[64px] lg:self-end">
      {title}
    </h2>
  );
};

export default HeadingComponent;
